127.0.0.1	localhost
::1		localhost ip6-localhost ip6-loopback
ff02::1		ip6-allnodes
ff02::2		ip6-allrouters

127.0.1.1 {{ inventory_hostname_short }}.{{ cluster_domain }} {{ inventory_hostname_short }}
{% for host in groups['loadbalancers'] %}
{{ target_hosts[host].ip }} {{ host }}.{{ cluster_domain }} {{ host }}
{{ target_hosts[host].ipv6 }} {{ host }}.{{ cluster_domain }} {{ host }}
{% endfor %}
{% for host in groups['servers'] %}
{{ target_hosts[host].ip }} {{ host }}.{{ cluster_domain }} {{ host }}
{{ target_hosts[host].ipv6 }} {{ host }}.{{ cluster_domain }} {{ host }}
{% endfor %}
{% for host in groups['agents'] %}
{{ target_hosts[host].ip }} {{ host }}.{{ cluster_domain }} {{ host }}
{{ target_hosts[host].ipv6 }} {{ host }}.{{ cluster_domain }} {{ host }}
{% endfor %}
